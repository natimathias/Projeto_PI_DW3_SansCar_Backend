import { LocacaoRepository } from "../locacoes/locacoes.repository";

export class MultaService {
    static async gerarMulta(id_locacao){
        const locacao = await LocacaoRepository.buscarPorId
    }
}